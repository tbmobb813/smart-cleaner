# Development dependencies for Smart Cleaner

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# Linting and type checking
ruff>=0.1.0
mypy>=1.5.0
# types-click pinned removed: no `types-click>=8.0.0` release on PyPI.
# Click 8+ includes type hints; if you need stubs for older Click, pin `types-click==7.1.8`.

# Code formatting
black>=23.7.0

# Pre-commit hooks (optional)
pre-commit>=3.3.0
